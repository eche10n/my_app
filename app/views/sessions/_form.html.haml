= form_for :session, url:sessions_path do |f|
  .field
    = f.label t('.email')
    = f.text_field :email
  .field
    = f.label t('.password')
    = f.password_field :password

  .actions
    = f.submit t('.sign_in')